# 2026-05-01

## Triad.build 模型切换

- 将 `scripts/update-calendar.ts` 中的 AI 模型从 `qwen-plus` 改为 `qwen-flash`
- 注意：`qwen-flash` 是正确的 API model name（DashScope），不是 `qwen-turbo`
- qwen-flash 价格更低（¥0.15/百万输入 token），适合结构化提取场景
