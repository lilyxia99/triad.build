# 2026-05-19 Daily Log

## Fixed GitHub Actions AI API Timeout Issue

### Problem
GitHub Actions workflow was failing with `ETIMEDOUT` error when connecting to Qwen API (Chinese server `token-plan.cn-beijing.maas.aliyuncs.com`).

Error message:
```
APIConnectionError: Connection error.
code: 'ETIMEDOUT'
```

### Solution Applied
1. **Added timeout configuration** to OpenAI client (120 seconds)
2. **Added retry logic with exponential backoff** for AI API calls
   - Max retries: 3
   - Base wait: 30s (30s, 60s, 90s)
   - Handles: `ETIMEDOUT`, `ECONNREFUSED`, `ECONNRESET`, connection timeout errors
3. **Model name is correct**: `qwen3.6-plus` (user confirmed, account has balance)

### Files Modified
- `scripts/update-calendar.ts`
  - Added `AI_RETRY_MAX` and `AI_RETRY_BASE_WAIT` constants
  - Added `retryAICall()` helper function with exponential backoff
  - Added `timeout: 120000` to OpenAI client config
  - Modified `doOCRWithQwen()` to use retry logic
  - Modified `analyzeWithAI()` to use retry logic
  - Confirmed `AI_MODEL_NAME` is `qwen3.6-plus` (correct)

### Commits
- `282264c`: "fix: Add retry logic and timeout for AI API calls"
- `a9e289c`: "fix: Revert model name to qwen3.6-plus (correct model name)"

### Next Steps
- Monitor GitHub Actions workflow to verify the retry logic resolves the timeout issue
- If connection still fails, consider using a proxy or switching to a more stable API endpoint
