# 2026-04-12

## 项目改动

### 替换 OpenAI → 通义千问 Qwen-plus

用户选择将 `scripts/update-calendar.ts` 中的 OpenAI API 替换为**阿里云百炼通义千问 qwen-plus**，原因：OpenAI API Key 过期。

**改动文件：**
- `scripts/update-calendar.ts`：环境变量 `OPENAI_API_KEY` → `DASHSCOPE_API_KEY`，baseURL 改为阿里云百炼端点，模型从 `gpt-4o-mini` 改为 `qwen-plus`
- `sample.env`：`OPENAI_API_KEY` → `DASHSCOPE_API_KEY`

**代码完全兼容 OpenAI SDK，改动仅 4 处。**

**待完成：**
- 用户需在阿里云百炼 (bailian.console.aliyun.com) 创建 API-KEY
- 在 GitHub Secrets / .env 中设置 `DASHSCOPE_API_KEY`
- 可享受 100万输入+100万输出 token 免费额度（90天内）
